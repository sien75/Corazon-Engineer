# AGENTS.md — Corazon Engineer

> 本文件是中文镜像,以英文版 `AGENTS.md` 为准。

本仓库就是 **Corazon Engineer** 本体。本文件告诉 AI 如何**开发本仓库**。

> 随工具发布的 AI 能力规范是 `agents/AGENTS.md`(中文在 `agents/zh/`)—— 面向对象不同:它描述如何*使用* Corazon,以及如何开发一个 Corazon-like 项目。

## 开发流程 SOP

每个任务都必须按顺序经历以下四个阶段,不允许跳过任何阶段。

### 阶段一:澄清需求

第一步永远是和用户沟通,绝不基于假设直接写代码。

- 先阅读相关的 schema / 代码建立上下文,然后再和用户沟通。
- 持续追问,直到目标、范围和验收标准都明确无歧义。
- 确认哪些内容明确**不在范围内**。
- 在动手前,把确认后的需求总结给用户。如果对需求的把握低于 80%,继续问,不要猜。

**完成标准:** 用户确认了需求总结。

### 阶段二:开发 & 测试(反复验证)

小步开发,每走一步立即验证。开发和测试是一个循环,而不是两个先后顺序的步骤。

1. 实现最小有意义改动。
2. 立即运行 / 测试(单测、手动运行或 schema 查询——视改动而定)。
3. 失败则读错误信息、修复、重测,直到通过。
4. 同一个失败连续出现 **3 次**时,立即停止。不要做第 4 次盲目重试——向用户报告错误、已尝试的方案和疑似阻塞点,并给出备选思路。
5. 只做最小改动,不顺手重构无关代码,不做超出需求的东西。

**完成标准:** 相关测试 / 检查全部通过,且改动符合已确认的需求。

### 阶段三:人工审核卡点(强制)

这是硬性卡点。AI 必须在这里停下,等待人工明确确认。

- 简单总结改动即可(改了什么 + 测试结论),不要长篇罗列。
- 提示用户去内容区域查看实际变动结果(diff / 改动的文件)。
- 在用户明确批准之前,**不得**进入部署阶段。
- 如果用户提出修改意见,带着反馈回到阶段二。

**完成标准:** 人工明确批准(如 "LGTM"、"可以"、"没问题,部署吧")。

### 阶段四:部署

对 Corazon 项目来说,部署就是**推代码**(commit + push 到仓库)。

- 只有在阶段三获批后才能部署。
- 用简洁的 commit message 描述改动,然后 push。
- push 完成后向用户报告结果(commit hash / 推了什么)。
- 如果 push 失败,报告错误——未经明确许可不得 force-push 或改写 git 历史。

**完成标准:** 代码已推送,且已告知用户。

## 仓库结构

- `devtime/` —— 开发时资料(不发布):`architecture/`(需求与设计、迭代记录)、`coding/`(代码 + 单元测试)、`deploy/`(构建 / 打包 / 启动 / 部署)。
- `runtime/` —— 运行系统资料(发布):`testing/`(E2E 测试)、`operation/`(连接 / 观测资源)。
- `agents/` —— 随工具发布的 AI 能力规范(`agents/AGENTS.md`,中文在 `agents/zh/`),以及 schema / contract / enum 参考。
- `atoms/` `edges/` `contracts/` `docs/` `notes/` `workspace/` —— 本项目的 schema 与代码。
</content>
