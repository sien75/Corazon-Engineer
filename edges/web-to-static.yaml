edges:
  - id: web-to-static
    from: web
    from_interface: static-query
    to: static
    to_interface: static-query
    channel: network
    protocol: http
    description: Web frontend pulls the full schema (atoms + edges) from static to render the architecture graph
  - id: web-to-static-detail
    from: web
    from_interface: static-query-detail
    to: static
    to_interface: static-query-detail
    channel: network
    protocol: http
    description: Web frontend fetches section content (runtime/contracts/devtime/docs/notes) for the top menu views
  - id: web-to-static-stream
    from: web
    from_interface: static-stream
    to: static
    to_interface: static-stream
    channel: network
    protocol: http
    description: Web frontend subscribes to schema file-change events and reloads the graph / views live
